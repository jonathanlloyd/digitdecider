# DigitDecider

Learning project for training my first neural network - an MLP MNIST digit
classifier.

## Goals

- Pytorch primitives only (tensors, autograd) - trying to learn.
- Usable inference demo at the end - serve the model over fastapi, html canvas
  app that allows the user to draw and tries to classify the digit.
